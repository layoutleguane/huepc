---
import Alpaka from '../public/coolalpaka.png';

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="de">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" href="/favicon.png" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<header>
			<img src={Alpaka} alt="image description" width="300" />
			<h1>HüllerPolitCamp</h1>

		</header>
		<slot />
	</body>
</html>
<style is:global>
	:root {
		--accent: 124, 58, 237;
		--accent-gradient: linear-gradient(45deg, rgb(var(--accent)), #da62c4 30%, white 60%);
	}
	html {
		font-family: system-ui, sans-serif;
		background-color: #ffffff;
	}
	body {
		padding: 1rem 2rem;
		max-width: 1600px;
		margin: auto;
	}

	header {
		display: flex;
		flex-direction: row;
		gap: 2rem;
		align-items: center;
	}

	header img {
		height: 10rem;
		width: auto;
	}

	header h1 {
		font-size: 5rem;
		font-weight: 800;
		line-height: 1;
		margin: 0;
	}
</style>
